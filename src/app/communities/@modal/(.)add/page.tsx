/**
 * Add Community Modal (Intercepting Route)
 * Shows as modal when navigating from /communities
 */

import { AddCommunityModalWrapper } from '@/components/communities/AddCommunityModalWrapper';

export default function AddCommunityModalPage() {
  return <AddCommunityModalWrapper />;
}
